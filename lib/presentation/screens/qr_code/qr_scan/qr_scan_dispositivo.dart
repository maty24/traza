import 'package:flutter/material.dart';



class ScanDispositivo extends StatelessWidget {
  const ScanDispositivo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}