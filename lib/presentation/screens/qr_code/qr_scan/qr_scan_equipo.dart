import 'package:flutter/material.dart';

class ScanEquipo extends StatelessWidget {
  const ScanEquipo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
